<!DOCTYPE html>
<html>
<body>


    <h1>Mã bill: {{ bills.code }}</h1>
    <h1>Ngày cưới: {{ cart.wedding_date }}</h1>
    <h1>Ngày giao: {{ first_clothe.delivery_date }}</h1>
    <h1>Ngày trả: {{ first_clothe.return_date }}</h1>
    <h1>Tên Khách hàng: {{ cart.client.full_name }}</h1>
    <h1>Địa chỉ: {{ cart.client.address}}</h1>
    <h1>Điện thoại: {{ cart.client.phone}}</h1>
    <h1>Ngày cọc: {{ bills.created_at}}</h1>
  
    <ul>
        {% if items.clothe_bill %}
          <li>Cho thuê Áo cưới:</li>
          <ul>
            {% for clothe in items.clothe_bill %}
              {% if clothe %}
                {% for item in clothe %}
                  <li>Tên {{ item.clothe.name }}; Mã {{ item.clothe.code }} giá lẻ {{ item.price }} giảm giá {{ item.discount }}</li>
                {% endfor %}
              {% endif %}
              
            {% endfor %}
          </ul>
        {% endif %}
    </ul>
    
    </ul>
        {% if items.makup_bill %}
            <li>Trang điểm cô dâu:</li>
            <ul>
            {% for makeup in items.makup_bill %}
                {% if makeup %}
                    {% for item in makeup %}
                        <li>Tên {{ item.package.name}} giá lẻ {{ item.price }} giảm giá {{ item.discount }}</li>
                        Dặm lại: {{ item.package.re_makup}}
                    {% endfor %}
                {% endif %}
            {% endfor %}
          </ul>
        {% endif %}
      </ul>
      
    <ul>
        {% if items.photo_bill %}
            <li>Chụp Ảnh Cưới:</li>
            <ul>
            {% for photo in items.photo_bill %}
                {% if photo %}
                    {% for item in photo %}
                        <li>{{ item.package.name }} giá lẻ {{ item.price }} giảm giá {{ item.discount }}</li>
                        Điểm chụp: {{ item.package.number_location }}
                        Hình cổng: {{ item.package.number_gate_photo }}
                        Hình nhỏ: {{ item.package.number_small_photo }}
                        File gốc: {{ item.package.origin_file }}
                        File chỉnh sửa: {{ item.package.edit_file }}
                        Có ambum: {{ item.package.is_album }}

                    {% endfor %}
                {% endif %}
            {% endfor %}
            </ul>
        {% endif %}
    </ul>
  
  <h1>Tổng tiền giá bán lẻ:{{total_retail}}</h1>
  <h1>Tổng giảm giá:{{discount}}</h1>
  <h1>Tổng phát sinh:{{incurred}}</h1>
  <h1>Tổng tiền thanh toán:{{total}}</h1>
  <h1>Tổng tiền đã trả:{{paid}}</h1>
  <h1>Tổng tiền còn lại:{{receivable}}</h1>
</body>
</html